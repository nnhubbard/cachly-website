{
 "id": "multi-user-logging",
 "title": "Multi-User Logging",
 "section": "Cachly Pro",
 "url": "multi-user-logging.html",
 "lead": "Cache as a couple, family or team and log everyone’s find in one pass — each account with its own message, favorites and trackables. Added in Cachly 8.2 for Pro subscribers.",
 "sections": [
  {
   "anchor": "setup",
   "heading": "Setting Up Users",
   "text": "Accounts are managed in Settings › Users : Primary User — the account used for all of Cachly, including searching for caches. Other Users — additional accounts used for logging. Tap + to add one; each signs in through geocaching.com’s own authorization page, so Cachly never sees their password. Each user’s … menu offers Set as Primary , their own Text Templates , Auto-Visit Trackables , and Delete . Multi-User Logging toggle — when enabled, logging a cache lets you submit logs for all added users at once."
  },
  {
   "anchor": "set-as-primary",
   "heading": "Switching Accounts with Set as Primary",
   "text": "👥 Set as Primary switches all of Cachly to another account in one tap. Open Settings › Users , tap the … button beside any other user and choose Set as Primary . That account becomes the one Cachly uses for everything — searching, the map, lists, the profile, logging — and the previous primary moves down into Other Users . For example, set your partner’s account as primary, then open the Logs tab to see their logs, drafts and pending logs instead of yours. Both accounts stay signed in, so switching back is the same one tap on the old primary’s menu — no need to authorize again. Cachly refreshes the profile, premium status and avatar for the new primary as soon as you switch."
  },
  {
   "anchor": "per-user-templates",
   "heading": "Text Templates for Each User",
   "text": "ℹ Every account has its own set of text templates, and you edit them in two different places. The primary user’s templates live in Settings › Templates (or tap Edit Text Templates in the log message editor). Each other user’s templates are edited from Settings › Users : tap their … button and choose Text Templates to open that account’s own template list, with the same Edit, Set as Default, Reorder and Delete options described under Log Templates . When you log with several users, each account’s own templates are inserted into its message section automatically, based on the log type — so a family can each keep their own sign-off."
  },
  {
   "anchor": "how-it-works",
   "heading": "How It Works",
   "text": "Cachly can log a single cache under multiple geocaching.com accounts at the same time — ideal for couples, families or groups who find together. With users added, the log screen gains an Other Users section where every account appears with its own controls: Choose who’s logging — each additional account has a checkmark beside its name; tap to include or exclude that person from this log. Your own account is always logged. Tap an account’s row to expand its options. Per-account details — every selected account gets its own message , Favorite Cache toggle, Save as Draft toggle and trackables , set independently. Each account’s own text templates are filled in automatically. Shared settings — the date , log type and (when updating) the coordinates apply to everyone at once. Favorite points are a premium-member feature, so the toggle only appears for accounts that are geocaching.com Premium (and it’s hidden entirely on event caches, where it doesn’t apply). When you tap Send, Cachly submits a separate, real log for each selected account. 👥 Multi-user logging applies when you create a log. Editing an existing pending log is single-account. Each added account must stay signed in — if its session expires, re-add it under Settings › Users before logging."
  }
 ],
 "screenshots": [
  {
   "file": "settings-users.png",
   "caption": "Settings › Users — the Primary User, additional users for logging, and the Multi-User Logging toggle.",
   "anchor": "setup"
  },
  {
   "file": "settings-users-menu-crop.png",
   "caption": "Each user’s menu: Set as Primary, Text Templates, Auto-Visit Trackables, Delete.",
   "anchor": "setup"
  },
  {
   "file": "log-compose-multi-2.png",
   "caption": "The log screen with an Other Users section listing every added account.",
   "anchor": "how-it-works"
  },
  {
   "file": "log-compose-multi-expanded.png",
   "caption": "Expanding a user reveals their own Message, Favorite Cache, Save as Draft and Trackables.",
   "anchor": "how-it-works"
  }
 ],
 "html": "<p class=\"lead\">Cache as a couple, family or team and log everyone’s find in one pass — each account with its own message, favorites and trackables. Added in Cachly 8.2 for Pro subscribers.</p>\n        <h2 id=\"setup\">Setting Up Users</h2>\n<div class=\"shots\">\n<figure class=\"shot\"><img loading=\"lazy\" src=\"assets/screenshots/settings-users.png\" alt=\"Settings &amp;rsaquo; Users &amp;mdash; the Primary User, additional users for logging, and the Multi-User Logging toggle.\" width=\"248\"><figcaption>Settings &rsaquo; Users &mdash; the Primary User, additional users for logging, and the Multi-User Logging toggle.</figcaption></figure>\n<figure class=\"shot\"><img loading=\"lazy\" src=\"assets/screenshots/settings-users-menu-crop.png\" alt=\"Each user&amp;rsquo;s menu: Set as Primary, Text Templates, Auto-Visit Trackables, Delete.\" width=\"248\"><figcaption>Each user&rsquo;s menu: Set as Primary, Text Templates, Auto-Visit Trackables, Delete.</figcaption></figure>\n</div>\n        <p>Accounts are managed in <span class=\"ui-path\">Settings &rsaquo; Users</span>:</p>\n        <ul>\n          <li><strong>Primary User</strong> &mdash; the account used for all of Cachly, including searching for caches.</li>\n          <li><strong>Other Users</strong> &mdash; additional accounts used for logging. Tap <strong>+</strong> to add one; each signs in through geocaching.com&rsquo;s own authorization page, so Cachly never sees their password. Each user&rsquo;s <strong>&hellip;</strong> menu offers <a href=\"#set-as-primary\"><strong>Set as Primary</strong></a>, their own <a href=\"#per-user-templates\"><strong>Text Templates</strong></a>, <strong>Auto-Visit Trackables</strong>, and <strong>Delete</strong>.</li>\n          <li><strong>Multi-User Logging</strong> toggle &mdash; when enabled, logging a cache lets you submit logs for all added users at once.</li>\n        </ul>\n\n        <h2 id=\"set-as-primary\">Switching Accounts with Set as Primary</h2>\n        <div class=\"callout tip\">\n          <span class=\"callout-icon\" aria-hidden=\"true\">&#128101;</span>\n          <div class=\"callout-body\"><strong>Set as Primary switches all of Cachly to another account in one tap.</strong> Open <span class=\"ui-path\">Settings &rsaquo; Users</span>, tap the <strong>&hellip;</strong> button beside any other user and choose <strong>Set as Primary</strong>. That account becomes the one Cachly uses for <em>everything</em> &mdash; searching, the map, lists, the profile, logging &mdash; and the previous primary moves down into <strong>Other Users</strong>. For example, set your partner&rsquo;s account as primary, then open the <strong>Logs</strong> tab to see <em>their</em> logs, drafts and pending logs instead of yours.</div>\n        </div>\n        <p>Both accounts stay signed in, so switching back is the same one tap on the old primary&rsquo;s menu &mdash; no need to authorize again. Cachly refreshes the profile, premium status and avatar for the new primary as soon as you switch.</p>\n\n        <h2 id=\"per-user-templates\">Text Templates for Each User</h2>\n        <div class=\"callout note\">\n          <span class=\"callout-icon\" aria-hidden=\"true\">&#8505;</span>\n          <div class=\"callout-body\"><strong>Every account has its own set of text templates, and you edit them in two different places.</strong> The <strong>primary user&rsquo;s</strong> templates live in <span class=\"ui-path\">Settings &rsaquo; Templates</span> (or tap <strong>Edit Text Templates</strong> in the log message editor). Each <strong>other user&rsquo;s</strong> templates are edited from <span class=\"ui-path\">Settings &rsaquo; Users</span>: tap their <strong>&hellip;</strong> button and choose <strong>Text Templates</strong> to open that account&rsquo;s own template list, with the same Edit, Set as Default, Reorder and Delete options described under <a href=\"logging.html#templates\">Log Templates</a>.</div>\n        </div>\n        <p>When you log with several users, each account&rsquo;s own templates are inserted into <em>its</em> message section automatically, based on the log type &mdash; so a family can each keep their own sign-off.</p>\n\n        <h2 id=\"how-it-works\">How It Works</h2>\n<div class=\"shots\">\n<figure class=\"shot\"><img loading=\"lazy\" src=\"assets/screenshots/log-compose-multi-2.png\" alt=\"The log screen with an Other Users section listing every added account.\" width=\"248\"><figcaption>The log screen with an Other Users section listing every added account.</figcaption></figure>\n<figure class=\"shot\"><img loading=\"lazy\" src=\"assets/screenshots/log-compose-multi-expanded.png\" alt=\"Expanding a user reveals their own Message, Favorite Cache, Save as Draft and Trackables.\" width=\"248\"><figcaption>Expanding a user reveals their own Message, Favorite Cache, Save as Draft and Trackables.</figcaption></figure>\n</div>\n        <p>Cachly can log a single cache under <strong>multiple geocaching.com accounts at the same time</strong> &mdash; ideal for couples, families or groups who find together. With users added, the log screen gains an <strong>Other Users</strong> section where every account appears with its own controls:</p>\n        <ul>\n          <li><strong>Choose who&rsquo;s logging</strong> &mdash; each additional account has a <strong>checkmark</strong> beside its name; tap to include or exclude that person from this log. Your own account is always logged. Tap an account&rsquo;s row to expand its options.</li>\n          <li><strong>Per-account details</strong> &mdash; every selected account gets its own <strong>message</strong>, <strong>Favorite Cache</strong> toggle, <strong>Save as Draft</strong> toggle and <strong>trackables</strong>, set independently. Each account&rsquo;s own <a href=\"logging.html#templates\">text templates</a> are filled in automatically.</li>\n          <li><strong>Shared settings</strong> &mdash; the <strong>date</strong>, <strong>log type</strong> and (when updating) the <strong>coordinates</strong> apply to everyone at once.</li>\n        </ul>\n        <p>Favorite points are a premium-member feature, so the toggle only appears for accounts that are geocaching.com Premium (and it&rsquo;s hidden entirely on event caches, where it doesn&rsquo;t apply). When you tap Send, Cachly submits a separate, real log for each selected account.</p>\n\n        <div class=\"callout note\">\n          <span class=\"callout-icon\" aria-hidden=\"true\">&#128101;</span>\n          <div class=\"callout-body\">Multi-user logging applies when you <em>create</em> a log. Editing an existing <a href=\"logs.html#pending\">pending log</a> is single-account. Each added account must stay signed in &mdash; if its session expires, re-add it under <span class=\"ui-path\">Settings &rsaquo; Users</span> before logging.</div>\n        </div>\n"
}